/**
 * Scripts inside components need to be careful
 * Probably best to restrict them to listening to core events
 * They'll get smashed into a large index.js file so watch out for conflicts
 */

(function() {
    'use strict';

    console.log( 'Nav component ready' );
})();
